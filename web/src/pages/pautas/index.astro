---
import Base from '../../layouts/Base.astro';
import DocList from '../../components/DocList.astro';
import { getCollection } from 'astro:content';
import { sections } from '../../lib/sections';
const s = sections.pautas;
const entries = await getCollection('pautas');
---
<Base title={s.label} description={s.intro}>
  <section class="section"><div class="wrap">
    <div class="sec-head narrow"><p class="eyebrow">Planejamento</p><h1>{s.label}</h1><p class="lead">{s.intro}</p></div>
    <DocList section={s} entries={entries} />
  </div></section>
</Base>
